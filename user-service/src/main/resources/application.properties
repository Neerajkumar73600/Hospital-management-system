server.port=8081

spring.datasource.url=jdbc:mysql://localhost:3306/pulse_hms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Ab12@@##

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

app.jwt.secret=pulse_hms_super_secret_key_minimum_32_chars_long
app.jwt.expiration=86400000
```

?? Replace `YOUR_MYSQL_PASSWORD` with your actual MySQL password.

---

## STEP 7 ? Run It!

Click the green **? button** next to `UserServiceApplication` in IntelliJ.

Watch the console ? you should see:
```
Started UserServiceApplication on port 8081